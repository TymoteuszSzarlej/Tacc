{% extends "base.html" %}

{% block content %}
<h2>📚 Księgi ({{ books|length }})</h2>
<div class="container" style="margin-top: 20px;">
    {% for book in books %}
    <div class="card">
        <h3>{{ book.name }}</h3>
        <p><strong>Opis:</strong> {{ book.description|default:"Brak opisu" }}</p>
        <p><strong>Data utworzenia:</strong> {{ book.created_at|date:"Y-m-d H:i" }}</p>
        <p><strong>Data ostatniej aktualizacji:</strong> {{ book.updated_at|date:"Y-m-d H:i" }}</p>
        <div class="actions" style="margin-top: 10px;">
            <a href="{% url 'book_details' book.id %}" class="btn btn-info">👀 Szczegóły</a>
            <a href="{% url 'edit_book' book.id %}" class="btn btn-primary">✏️ Edytuj</a>
            <a href="{% url 'delete_book' book.id %}" class="btn btn-danger">🗑️ Usuń</a>
        </div>
    </div>
    {% empty %}
    <p>Nie masz jeszcze żadnych ksiąg. Kliknij poniżej, aby dodać pierwszą.</p>
    {% endfor %}
    <div style="text-align: center; margin-top: 30px;">
        <a href="{% url 'create_book' %}" class="btn btn-success">➕ Dodaj nową księgę</a>
    </div>
</div>
{% endblock %}
