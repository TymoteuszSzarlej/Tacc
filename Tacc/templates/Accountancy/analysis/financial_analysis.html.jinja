{% extends 'base.html' %}

{% block content %}
<div class="container">
    <h2>📊 Analiza Finansowa</h2>

    <h3>📈 Rentowność</h3>
    <div class="card">
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>Wskaźnik</th>
                    <th>Wartość</th>
                    <th>Poziom</th>
                    <th>Interpretacja</th>
                </tr>
            </thead>
            <tbody>
                <tr class="{% if ros > 0 %}gain{% else %}loss{% endif %}">
                    <td>Rentowność sprzedaży (ROS)</td>
                    <td>{{ ros|floatformat:2 }}%</td>
                    <td>{% if ros > 0 %} ✅ {% else %} ❌ {% endif %}</td>
                    <td>
                        {% if ros <= 0 %}
                            Ujemny wynik oznacza, że Twoje wydatki przewyższają dochody z codziennych źródeł...
                        {% else %}
                            Dodatnia rentowność pokazuje, że po opłaceniu wszystkich codziennych wydatków...
                        {% endif %}
                    </td>
                </tr>
                <tr class="{% if roa > 0 %}gain{% else %}loss{% endif %}">
                    <td>Rentowność aktywów (ROA)</td>
                    <td>{{ roa|floatformat:2 }}%</td>
                    <td>{% if roa > 0 %} ✅ {% else %} ❌ {% endif %}</td>
                    <td>
                        {% if roa <= 0 %}
                            Ujemny wskaźnik pokazuje, że posiadany majątek...
                        {% else %}
                            Dodatni wynik oznacza, że majątek osobisty jest wykorzystywany efektywnie...
                        {% endif %}
                    </td>
                </tr>
                <tr class="{% if roe > 0 %}gain{% else %}loss{% endif %}">
                    <td>Rentowność kapitału własnego (ROE)</td>
                    <td>{{ roe|floatformat:2 }}%</td>
                    <td>{% if roe > 0 %} ✅ {% else %} ❌ {% endif %}</td>
                    <td>
                        {% if roe <= 0 %}
                            Ujemny ROE oznacza, że Twoje oszczędności i środki własne nie przynoszą zwrotu...
                        {% else %}
                            Dodatni wskaźnik oznacza, że środki własne generują zyski...
                        {% endif %}
                    </td>
                </tr>
            </tbody>
        </table>
    </div>

    <h3>💧 Płynność finansowa</h3>
    <div class="card">
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>Wskaźnik</th>
                    <th>Wartość</th>
                    <th>Poziom</th>
                    <th>Interpretacja</th>
                </tr>
            </thead>
            <tbody>
                <tr class="{% if current_ratio >= 1.2 and current_ratio <= 2.0 %}gain{% else %}loss{% endif %}">
                    <td>Wskaźnik bieżącej płynności</td>
                    <td>{{ current_ratio|floatformat:2 }}</td>
                    <td>{% if current_ratio >= 1.2 and current_ratio <= 2.0 %} ✅ {% else %} ❌ {% endif %}</td>
                    <td>
                        {% if current_ratio < 1.2 %}
                            Niski poziom oznacza trudność z pokrywaniem bieżących wydatków...
                        {% elif current_ratio > 2.0 %}
                            Zbyt wysoki poziom wskazuje na nadmiar gotówki...
                        {% else %}
                            Optymalny wynik oznacza zdrową równowagę...
                        {% endif %}
                    </td>
                </tr>
                <tr class="{% if quick_ratio >= 1.0 and quick_ratio <= 1.5 %}gain{% else %}loss{% endif %}">
                    <td>Wskaźnik szybkiej płynności</td>
                    <td>{{ quick_ratio|floatformat:2 }}</td>
                    <td>{% if quick_ratio >= 1.0 and quick_ratio <= 1.5 %} ✅ {% else %} ❌ {% endif %}</td>
                    <td>
                        {% if quick_ratio < 1.0 %}
                            Niski wskaźnik oznacza niewystarczające rezerwy...
                        {% elif quick_ratio > 1.5 %}
                            Wysoki wynik pokazuje duże rezerwy...
                        {% else %}
                            Optymalny poziom oznacza kontrolę nad płynnymi środkami...
                        {% endif %}
                    </td>
                </tr>
            </tbody>
        </table>
    </div>

    <h3>⚖️ Zadłużenie</h3>
    <div class="card">
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>Wskaźnik</th>
                    <th>Wartość</th>
                    <th>Poziom</th>
                    <th>Interpretacja</th>
                </tr>
            </thead>
            <tbody>
                <tr class="{% if debt_ratio < 60 %}gain{% else %}loss{% endif %}">
                    <td>Wskaźnik ogólnego zadłużenia</td>
                    <td>{{ debt_ratio|floatformat:2 }}%</td>
                    <td>{% if debt_ratio < 60 %} ✅ {% else %} ❌ {% endif %}</td>
                    <td>
                        {% if debt_ratio >= 60 %}
                            Wysoki poziom zadłużenia oznacza duże obciążenie kredytami...
                        {% else %}
                            Niski poziom zadłużenia świadczy o bezpieczeństwie finansowym...
                        {% endif %}
                    </td>
                </tr>
            </tbody>
        </table>
    </div>
</div>
{% endblock %}
